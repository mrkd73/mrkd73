## الان کجاییم
نسخه **0.20.1** — سیاست نظر ایجنت اعمال شد:

**سریع (بدون تأیید):** فیلدهای صفحه/محصول (عنوان، قیمت، موجودی، برچسب، تصویر شاخص، متای allowlist، REST PUT/PATCH روی همان id)، تزریق خودکار postId از صفحهٔ زنده  

**با تأیید UI:** قالب/افزونه (`write_file`)، `custom_css`، optionهای غیرمجاز، trash، DELETE، متای ناشناخته  

## تست
محصول باز → بگو عنوان/قیمت/موجودی را عوض کن (باید فوری)  
بگو رنگ قالب را عوض کن (باید دکمهٔ تأیید بیاید)  

## بعداً صحبت می‌کنیم
- **مهندسی پرامپت** — هنوز باز؛ فعلاً فقط در لیست؛ جزئیات و تصمیم‌گیری در گفتگوی بعدی
